To run the system, navigate to the directory in the command line, run it with python travelRS.py 
and open a browser window to http://localhost:5000/
Select a UserID from the dropdown list to select the active user.
Enter trip details using the dropdown lists on the next page to refine the recommendations.
Requirements: Python 3 or above, pip install numpy, pandas, scipy, flask

